name: CI

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3.4.0
    - name: Install
      run: npm install
    - name: Run Linter
      run: npm run lint
